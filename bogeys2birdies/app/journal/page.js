import { Page } from '../../components/SiteChrome'; import { ArticleCard, Eyebrow } from '../../components/UI';
import { defaultArticles, defaultPageSettings, defaultSiteSettings, formatArticleMeta } from '../../content/defaults';
import { getArticles, getPageSettings, getSiteSettings } from '../../sanity/lib/content'; import { imageUrl } from '../../sanity/lib/image';
import { metadataFrom } from '../../sanity/lib/metadata';
export async function generateMetadata(){const page=await getPageSettings('journal',defaultPageSettings.journal);return metadataFrom(page);}
export default async function Journal(){const [items,site,page]=await Promise.all([getArticles(defaultArticles),getSiteSettings(defaultSiteSettings),getPageSettings('journal',defaultPageSettings.journal)]);return <Page siteSettings={site}><main className="inner-page"><section className="page-hero"><Eyebrow>{page.eyebrow}</Eyebrow><h1>{page.title}</h1><p>{page.description}</p></section><section className="section-shell"><div className="article-grid">{items.map((item)=><ArticleCard key={item._id||item.slug} category={item.category} categorySlug={item.categorySlug} title={item.title} meta={formatArticleMeta(item)} image={imageUrl(item.coverImage)} href={`/journal/${item.slug}`}/>)}</div></section></main></Page>}
