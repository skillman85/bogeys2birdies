import {timingSafeEqual} from 'node:crypto';import {createClient} from '@sanity/client';import {revalidatePath} from 'next/cache';import {importSeasonData} from '../../../../sanity/lib/seasonImport';
export const runtime='nodejs';const maxBytes=2*1024*1024;const response=(body,status)=>Response.json(body,{status,headers:{'Cache-Control':'no-store','X-Content-Type-Options':'nosniff'}});function authorised(request){const expected=process.env.PRECISION_GOLF_SYNC_SECRET;const supplied=request.headers.get('authorization')?.replace(/^Bearer\s+/i,'')||'';if(!expected||!supplied)return false;const a=Buffer.from(expected),b=Buffer.from(supplied);return a.length===b.length&&timingSafeEqual(a,b)}export async function POST(request){if(!authorised(request))return response({error:'Unauthorised.'},401);if(!request.headers.get('content-type')?.startsWith('application/json'))return response({error:'Content-Type must be application/json.'},415);if(Number(request.headers.get('content-length')||0)>maxBytes)return response({error:'Payload is too large.'},413);if(!process.env.SANITY_WRITE_TOKEN)return response({error:'Season sync is not configured.'},503);let data;try{const raw=await request.text();if(Buffer.byteLength(raw)>maxBytes)return response({error:'Payload is too large.'},413);data=JSON.parse(raw)}catch{return response({error:'Invalid JSON.'},400)}try{const client=createClient({projectId:process.env.NEXT_PUBLIC_SANITY_PROJECT_ID||'kfysb6ye',dataset:process.env.NEXT_PUBLIC_SANITY_DATASET||'production',apiVersion:'2026-08-14',useCdn:false,token:process.env.SANITY_WRITE_TOKEN});const result=await importSeasonData(client,data);revalidatePath('/');revalidatePath('/data');return response({ok:true,...result,syncedAt:new Date().toISOString()},200)}catch(error){console.error('PrecisionGolf sync failed:',error);return response({error:error instanceof Error?error.message:'Season sync failed.'},400)}}
